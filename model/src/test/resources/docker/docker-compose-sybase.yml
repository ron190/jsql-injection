version: "3.7"
services:

  sybase:
    container_name: jsql-sybase
    ports:
      - "5000:5000"
    build:
      context: .
      dockerfile: Dockerfile.sybase
    networks:
      - jsql-network
    command: bash /sybase/start
    hostname: dksybase

networks:
  jsql-network: