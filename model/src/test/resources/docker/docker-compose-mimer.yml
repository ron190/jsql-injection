version: "3.7"
services:

  mimer:
    container_name: jsql-mimer
    ports:
      - "1360:1360"
    build:
      context: .
      dockerfile: Dockerfile.mimer
    environment:
      MIMER_SYSADM_PASSWORD: SYSADM
    networks:
      - jsql-network

networks:
  jsql-network: