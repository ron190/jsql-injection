version: "3.7"
services:

  db2:
    container_name: jsql-db2
    image: ibmcom/db2
    privileged: true
    ports:
      - "50011:50011"
    environment:
      LICENSE: accept
      DB2INST1_PASSWORD: test
      DBNAME: testdb
    networks:
      - jsql-network

networks:
  jsql-network: