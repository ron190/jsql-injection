version: "3.7"
services:

  clickhouse:
    container_name: jsql-clickhouse
    # local issue with 26.6: Illegal instruction
    image: clickhouse/clickhouse-server:26.5
    privileged: true
    ports:
      - "8123:8123"
    environment:
      - CLICKHOUSE_USER=dba
      - CLICKHOUSE_PASSWORD=dba
    networks:
      - jsql-network

networks:
  jsql-network: