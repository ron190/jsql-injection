tooltip: Replace equal sign '=' to 'like'
description: Replace equal sign '=' to 'like'
group: 
javascript: |
 var tampering = function(sql) {
    
     return sql
     
         // Require space encoding
         .replace(/=/g, '+like+')
 }