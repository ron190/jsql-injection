<?xml version="1.0" encoding="UTF-8"?>
<model>
    <tooltip>Transform SQL keywords to versioned comment, e.g /*!concat*/()</tooltip>
    <description>Transform SQL keywords to versioned comment, e.g /*!concat*/()</description>
    <group></group>
    <javascript>
var tampering = function(sql) {
    return sql.replace(
        /([^_])(concat|ifnull)(\/\*\*\/)*\(/gmi,
        '$1/*!$2*/$3('
    )
}
    </javascript>
</model>